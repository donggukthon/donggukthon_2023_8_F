import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { Flex } from './index'
import { Center } from '../Center'
import { Font } from '../Font'

<Meta
  title="ui-system/Flex"
  component={Flex}
  argTypes={{
    align: {
      name: 'align',
      description: 'align-items',
      options: [
        'center',
        'flex-start',
        'flex-end',
        'start',
        'end',
        'self-start',
        'self-end',
        'baseline',
        'normal',
        'stretch',
        '-moz-initial',
        'inherit',
        'initial',
        'revert',
        'unset',
      ],
      defaultValue: 'center',
    },
    justify: {
      name: 'justify',
      description: 'justify-content',
      options: [
        'center',
        'between',
        'around',
        'evenly',
        'stretch',
        'flex-start',
        'flex-end',
        'start',
        'end',
        'self-start',
        'self-end',
        'baseline',
        'normal',
        '-moz-initial',
        'inherit',
        'initial',
        'revert',
        'unset',
      ],
      defaultValue: 'center',
    },
    gap: {
      name: 'gap',
      description: '요소 사이 간격',
    },
    direction: {
      name: 'direction',
      description: 'flex-direction',
      options: ['row', 'column'],
    },
    reverse: {
      options: [false, true],
    },
    wrap: {
      name: 'wrap',
      description: 'flex-wrap',
      options: ['nowrap', 'wrap', 'wrap-reverse', '-moz-initial', 'inherit', 'initial', 'revert', 'unset'],
    },
    shrink: {
      name: 'shrink',
      description: 'flex-shrink',
    },
    flex: {
      name: 'flex',
      description: 'flex',
    },
  }}
/>

export const Template = (args) => <Flex {...args} />

# Flex

## Basic Usage

<Canvas>
  <Story
    name="Basic Usage"
    args={{
      width: 800,
      height: 400,
      direction: 'row',
      reverse: false,
      gap: 50,
    }}
  >
    {(props) => (
      <Flex {...props}>
        <Center width={100} height={100} style={{ backgroundColor: 'coral' }}>
          <Font type={'heading-18-medium'} color={'white'}>
            Box 1
          </Font>
        </Center>
        <Center width={100} height={100} style={{ backgroundColor: 'coral' }}>
          <Font type={'heading-18-medium'} color={'white'}>
            Box 2
          </Font>
        </Center>
        <Center width={100} height={100} style={{ backgroundColor: 'coral' }}>
          <Font type={'heading-18-medium'} color={'white'}>
            Box 3
          </Font>
        </Center>
      </Flex>
    )}
  </Story>
</Canvas>

<ArgsTable
  story="Basic Usage"
  include={['direction', 'reverse', 'align', 'justify', 'gap', 'wrap', 'shrink', 'flex']}
/>
