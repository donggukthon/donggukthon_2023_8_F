import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { Tab } from './Tab'
import PlusIcon from '@components/common/svgs/PlusIcon'
import { Space } from '@components/common/Space'
import { Box } from '@components/common/Box'

<Meta
  title="Tab/Tab"
  component={Tab}
  argTypes={{
    selected: {
      defaultValue: false,
      control: 'boolean',
    },
    underline: {
      defaultValue: false,
      control: 'boolean',
    },
    highlight: {
      defaultValue: false,
      control: 'boolean',
    },
    count: {
      defaultValue: undefined,
      control: 'number',
    },
    textSize: {
      defaultValue: 'sm',
      control: 'radio',
      options: ['xs', 'sm', 'md', 'lg'],
    },
  }}
/>

# Tab

탭 컴포넌트입니다.

## Basic Usage

<Canvas>
  <Story name="Basic Usage">{(props) => <Tab {...props}>Tab</Tab>}</Story>
</Canvas>

<ArgsTable story="Basic Usage" include={['selected', 'underline', 'count', 'highlight']} />
