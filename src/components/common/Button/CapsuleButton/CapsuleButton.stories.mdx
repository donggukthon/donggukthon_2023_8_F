import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { CapsuleButton } from './CapsuleButton'
import PlusIcon from '@components/common/svgs/PlusIcon'
import { Space } from '@components/common/Space'

<Meta
  title="Button/CapsuleButton"
  component={CapsuleButton}
  argTypes={{
    kind: {
      defaultValue: 'primary',
      control: 'radio',
      options: ['cta', 'primary', 'secondary-gray', 'secondary-accent'],
    },
    size: {
      defaultValue: 'md',
      control: 'radio',
      options: ['xs', 'sm', 'md', 'lg', 'xl'],
    },
    LeftIconComponent: {
      control: false,
    },
    RightIconComponent: {
      control: false,
    },
    full: {
      defaultValue: false,
      control: 'boolean',
    },
    disabled: {
      defaultValue: false,
      control: 'boolean',
    },
  }}
/>

# CapsuleButton

배경색이 있는 둥근 버튼 컴포넌트입니다.

## Basic Usage

<Canvas>
  <Story name="Basic Usage">{(props) => <CapsuleButton {...props}>버튼</CapsuleButton>}</Story>
</Canvas>

<ArgsTable story="Basic Usage" include={['kind', 'size']} />

## Icon

왼쪽이나 오른쪽에 아이콘을 표시할 수 있습니다.

<Canvas>
  <Story name="Icon">
    {(props) => (
      <div>
        <CapsuleButton LeftIconComponent={PlusIcon} {...props}>
          버튼
        </CapsuleButton>
        <Space height={8} />
        <CapsuleButton RightIconComponent={PlusIcon} {...props}>
          버튼
        </CapsuleButton>
      </div>
    )}
  </Story>
</Canvas>

## Interaction

hover, active, disabled 상태에 따라 글씨와 배경색상이 달라집니다.

<Canvas>
  <Story name="Interaction">{(props) => <CapsuleButton {...props}>버튼</CapsuleButton>}</Story>
</Canvas>

<ArgsTable story="Interaction" include={['disabled']} />
