import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs'
import { TextButton } from './TextButton'
import PlusIcon from '@components/common/svgs/PlusIcon'
import ArrowRightSmallIcon from '@components/common/svgs/ArrowRightSmallIcon'

<Meta
  title="Button/TextButton"
  component={TextButton}
  argTypes={{
    kind: {
      defaultValue: 'primary',
      options: ['accent', 'primary', 'secondary'],
    },
    size: {
      defaultValue: 'lg',
      options: ['sm', 'lg'],
    },
    LeftIconComponent: {
      control: false,
    },
    RightIconComponent: {
      control: false,
    },
    disabled: {
      defaultValue: false,
      control: 'boolean',
    },
    underline: {
      defaultValue: false,
      control: 'boolean',
    },
    children: {
      defaultValue: '버튼 라벨',
    },
  }}
/>

# TextButton

텍스트 버튼 컴포넌트 타입입니다.

## Basic Usage

<Canvas>
  <Story name="Basic Usage">{(props) => <TextButton {...props} />}</Story>
</Canvas>

<ArgsTable story="Basic Usage" include={['kind', 'size', 'disabled', 'underline']} />

## Icon

왼쪽이나 오른쪽에 아이콘을 표시할 수 있습니다.

<Canvas>
  <Story name="Icon">
    {(props) => (
      <div>
        <TextButton {...props} kind="accent" LeftIconComponent={PlusIcon} />
        <br />
        <TextButton {...props} RightIconComponent={ArrowRightSmallIcon} />
      </div>
    )}
  </Story>
</Canvas>
